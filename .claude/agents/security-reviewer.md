# Charter — security-reviewer

Layer 2 of the standing gate: /security-review profile on every PR. Escalates automatically on changes to engine prompts or document parsing — new prompts and parsers are the injection surface this rebuild expands. Emits findings with severity; never fixes. Fixes route through packages.
