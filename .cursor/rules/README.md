Rules in this folder are the only rules. During the agentic setup PR every pre-existing rule that referenced a funder template catalogue, template picker, hint map, archetype, readiness score, or a specific funder's section list was deleted; anything still needed lives in `AGENTS.md`.

Bugbot does not read this folder; its rules are in `.cursor/BUGBOT.md` files.
